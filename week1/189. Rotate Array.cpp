class Solution {public:    void rotate(vector<int>& nums, int k) {        // RUN TIME ERROR: 1. For each iteration, only move 1 step.        // Time O(n2); Space O(1)        // if (nums.empty() || nums.size() == 1) return;        // for (int iter = 0; iter < k; ++iter) {        //     int cur, pre;        //     pre = nums[0];        //     for (int i = 1; i < nums.size(); ++i) {        //         cur = nums[i];        //         nums[i] = pre;        //         pre = cur;        //         if (i == nums.size() - 1)         //         nums[0] = cur;        //     }        // }        // 2. Move k step using additional space        // Time O(n); Space O(n)        vector<int> ans(nums.size());        for (int i = 0; i < nums.size(); ++i) {            ans[(i+k)%nums.size()] = nums[i];        }        nums = ans;    }};